import styled from '@emotion/native';

const MainContainer = styled.View`
  display: flex;
  flex-grow: 1;
`;

const ContentWrapper = styled.View`
  display: flex;
  flex-grow: 1;
`;

const S = {
  MainContainer,
  ContentWrapper,
};

export default S;
