import styled from '@emotion/native';

const Container = styled.View`
  display: flex;
  flex: 1;
  gap: 40px;
`;

const SetProfileForm = styled.View`
  display: flex;
  justify-content: center;
  flex: 1;
  gap: 40px;
`;

const InputForm = styled.View`
  display: flex;
  gap: 18px;
`;

const S = {
  Container,
  SetProfileForm,
  InputForm,
};

export default S;
