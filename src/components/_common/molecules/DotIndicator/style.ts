import styled from '@emotion/native';

const Wrapper = styled.View`
  display: flex;
  flex-direction: row;
  justify-content: center;
  gap: 8px;

  width: 100%;
`;

const S = {
  Wrapper,
};

export default S;
